import express from "express";

const router = express.Router();

// router.post("/create", createTodo);
// router.post("/check/:id", auth, checkTodo);
// router.delete("/:id", auth, deleteTodo);

export default router;
